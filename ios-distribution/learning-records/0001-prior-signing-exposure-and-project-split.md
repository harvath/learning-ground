# Prior exposure to signing UI; two active projects; TestFlight is the live goal

At course start, the user disclosed: (1) already enrolled in a paid Apple Developer Program membership and has previously used Xcode's Signing & Capabilities tab — not a total beginner, so lessons should skip "how to install Xcode" and go straight to mechanics/why; (2) maintains both a native Swift/SwiftUI project and a separate Expo/React Native project, but the one currently being distributed is the Swift one — teach natively, Expo only as short delta call-outs; (3) the live, immediate distribution goal is TestFlight beta, not yet a public App Store release.

**Implications**: L0005's checklist stops at "tester has the build" rather than continuing into App Store review/marketing. A future mission extension (public App Store release) would be the natural next course once TestFlight is routine — see `reference/pipeline-map.html`'s "next" row.
